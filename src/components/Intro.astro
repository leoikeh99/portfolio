---
import "../styles/layoutStyles.css";
import "../styles/uiStyles.css";
---

<section id="intro">
  <div class="container">
    <div class="content">
      <h1>Leonard Ikeh</h1>
      <h2>FRONTEND DEVELOPER</h2>
      <p>
        Hello i'm Leonard, a fullstack web developer that very much loves
        building/creating software applications, this is my portfolio site where
        you will be able to see my works and be able to contact me if you like
        what you see.
      </p>
      <a class="btnLink" href="#what-i-do">Get Started</a>
    </div>
  </div>
</section>

<style>
  section {
    height: 97dvh;
    background-image: url("/images/pcs.jpg");
    background-size: cover;
    position: relative;
    z-index: 1;
  }

  section::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.7);
  }

  section .container {
    height: 100%;
  }

  section .content {
    height: 100%;
    display: grid;
    align-content: center;
    place-items: center;
    gap: 2.3rem;
    text-align: center;
    position: relative;
  }

  h1 {
    font-size: var(--fs-2xl);
    font-weight: var(--fw-bold);
    color: var(--clr-primary-400);
    letter-spacing: 6px;
    padding: 30px 40px;
    border: 2px solid var(--clr-primary-400);
  }

  h2 {
    font-size: var(--fs-sub-heading);
    font-weight: var(--fw-bold);
    color: var(--clr-neutral-100);
    transition: opacity 0.5s ease-in-out;
    /* animation: fadeOutAndIn 3s infinite; */
  }

  p {
    color: var(--clr-neutral-100);
    max-width: 620px;
  }

  @media (max-width: 424px) {
    h1 {
      font-size: var(--fs-xl);
      padding: 15px 20px;
    }

    h2 {
      font-size: var(--fs-lg);
    }
  }

  @keyframes fadeOutAndIn {
    0% {
      opacity: 1;
    }
    70% {
      opacity: 1;
    }
    85% {
      opacity: 0;
    }
    100% {
      opacity: 1;
    }
  }
</style>
